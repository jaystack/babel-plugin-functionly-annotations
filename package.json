{
  "name": "babel-plugin-functionly-annotations",
  "version": "0.0.1",
  "description": "An experimental babel transformer plugin for functionly annotations",
  "repository": "jaystack/babel-plugin-functionly-annotations",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib --copy-files",
    "clean": "rm -rf lib",
    "prepublish": "npm run clean && npm run build"
  },
  "keywords": [
    "es6",
    "es2015",
    "babel",
    "babel-plugin",
    "functionly",
    "annotation",
    "decorator"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-core": "^6.8.0",
    "babel-preset-es2015": "^6.6.0"
  },
  "dependencies": {
    "babel-generator": "^6.8.0"
  }
}
